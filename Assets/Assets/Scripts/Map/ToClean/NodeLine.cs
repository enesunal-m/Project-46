using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NodeLine : MonoBehaviour
{
     
    void Start()
    {
        if(this.name != "Node")
        {
        int x = (int)this.name[0];
        int y = (int)this.name[2];

        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
